# 依赖与构建产物
node_modules/
dist/
src-tauri/target/

# 日志
*.log

# 内部工作区（SDD 过程记录：裁决台账、各轮报告、真机验收证据/截图）。
# 里面有开发机路径与接管过程中的 settings.json 片段 —— **不要上传**。
# 内层 .superpowers/sdd/.gitignore 也写了 `*`，这里是第二道防线：
# 内层文件一旦被删/改，整个目录就会变成可提交状态。
.superpowers/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db
